PAYLOAD_SECRET=
NEXT_PUBLIC_SITE_URL=http://localhost:4173

# MongoDB — use ONE of these:
# Local (Docker):  mongodb://still:still_dev_password@mongo:27017/still?authSource=admin
# Atlas (Cloud):   mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/still
MONGODB_URI=

# Vercel Blob — leave empty for local disk storage (media/ folder)
BLOB_READ_WRITE_TOKEN=

# Mapbox — public token for map page (URL-restrict in Mapbox Dashboard)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Plausible Analytics — set domain to enable (e.g. still.at), leave empty to disable
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
